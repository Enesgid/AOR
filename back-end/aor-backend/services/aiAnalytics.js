const User = require("../models/user");
const Submission = require("../models/Submission");

const RETIREMENT_AGE = 65;

const getRetirementAlerts = async () => {
  try {
    
  } catch (error) {
    console.error(error);

    return [];
  }
};

module.exports = {
  getRetirementAlerts,
};